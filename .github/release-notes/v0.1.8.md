# v0.1.8

- Make the background scheduler opt-out during tests to eliminate configuration data races.
- Keep production scheduling enabled by default.
- Verify Settings can be saved to the host-mounted `/data/.vloader` directory.
