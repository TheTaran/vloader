# v0.1.4

- Persist Settings and catalog data inside the host-mounted `/data` volume.
- Store application state in `/data/.vloader` so the non-root process can write safely.
- Correct series-library counters to show only series titles.
