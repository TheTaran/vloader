# v0.1.5

- Fix read-only image startup checks after introducing persistent `/data/.vloader` state.
- Keep production persistence under `/data` while allowing security-focused container tests to run without a writable data mount.
